{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 9000",
    "dev": "yarn start"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
